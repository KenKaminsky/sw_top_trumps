// import '@testing-library/jest-dom/extend-expect';
// import { render, screen } from '@testing-library/react';
// import * as React from 'react';
// import CardDetails from './CardDetails';

// describe('<CardDetails />', () => {
//   describe('should show field data correctly', () => {
//     it.each([
//       [0, 0],
//       [100, 100],
//       [null, 'N/A'],
//       [undefined, 'N/A'],
//       ['unknown', 'unknown'],
//     ])('when value (%i) expects (%i)', (fieldValue, expeted) => {
//       const fieldName = 'height';
//       const expectedFieldTect = 'Height';

//       // render(<CardDetails fieldName={fieldName} fieldValue={fieldValue} />);

//       expect(screen.getByText(expectedFieldTect)).toBeInTheDocument();
//       expect(screen.queryByText(expeted)).toBeInTheDocument();
//     });
//   });

// });
